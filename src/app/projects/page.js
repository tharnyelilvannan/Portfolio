import { motion } from "motion/react"

export default function Page() {
    return (
        <div className="container min-h-screen min-w-screen bg-[#FFFFFF]">
            <h1 className="text-black absolute inset-y-20 left-10 font-[Roboto]">Work in Progress!</h1>
        </div>
    )
}